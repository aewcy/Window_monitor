# ============================================
# 被控端 (Agent) 依赖 - 运行在试验机上
# ============================================
mss>=9.0.0
Pillow>=10.0.0
psutil>=5.9.0
requests>=2.31.0

pynput>=1.7.6

# Windows 额外依赖 (setup.bat 会自动安装)
# pywin32>=306
