# ============================================
# 监控系统 Demo - 依赖清单 (跨平台)
# ============================================

# ---- Server (监控机) ----
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ---- Agent (试验机) ----
# 屏幕截图 (跨平台: Windows / Linux / macOS)
mss>=9.0.0
Pillow>=10.0.0

# 进程信息 (跨平台)
psutil>=5.9.0

# HTTP 客户端 - Agent 上传数据
requests>=2.31.0

# ============================================
# 平台特定依赖 - 按需安装
# ============================================

# Windows (试验机为Windows时安装)
# pywin32>=306

# Linux (试验机为Linux时安装) - 活动窗口检测
# python-xlib>=0.33
# 或者使用 xdotool 命令行工具 (系统包管理器安装)
#   CentOS/RHEL: yum install xdotool
#   Ubuntu/Debian: apt install xdotool
#   Fedora: dnf install xdotool

# ---- 可选 ----
# 加密 (如需加密传输)
# cryptography>=42.0.0
