# Dashboard 功能测试清单

每次改动后必须逐项验证。打勾 = 通过。

## 基础加载

- [ ] 访问 `/` 页面正常加载，无白屏
- [ ] 浏览器控制台无 JS 报错
- [ ] 背景光晕动画正常

## Agent 管理

- [ ] Agent 列表正确显示（名称、在线状态、间隔）
- [ ] 点击 Agent 切换，截图卡片更新
- [ ] 离线 Agent 显示灰色圆点
- [ ] 在线 Agent 显示绿色圆点 + 脉冲动画

## 截图 — 实时模式

- [ ] 选择 Agent 后自动加载最新截图
- [ ] 截图图片正常显示
- [ ] 右上角显示时间戳
- [ ] 右上角显示 FPS 标签（如 1fps、4fps）
- [ ] 多显示器时显示显示器切换芯片

## 截图 — 历史模式

- [ ] 切换到历史模式，加载历史截图列表
- [ ] 上一张/下一张按钮可切换截图
- [ ] 到达首张/末张时按钮不报错

## 截图 — Live 放大

- [ ] 点击「放大」按钮，弹出 70% 视口的 overlay
- [ ] overlay 内显示截图 + 时间戳
- [ ] 点击遮罩外部关闭
- [ ] 点击「关闭」按钮关闭
- [ ] 按 ESC 关闭

## 网格视图

- [ ] 顶部栏点击「网格视图」弹出 overlay
- [ ] 网格缩略图正常加载，间距正确不重叠
- [ ] 滚动到底部自动加载下一批（无限滚动）
- [ ] hover 显示复选框和删除按钮
- [ ] 点击复选框选中/取消选中
- [ ] 全选功能正常
- [ ] 选中后顶部显示「删除选中」按钮
- [ ] 单张删除 — 点击 × 按钮，确认弹窗，删除成功
- [ ] 批量删除 — 选中多张，确认弹窗，删除成功
- [ ] 按 ESC 关闭网格 overlay

## 活动记录

- [ ] 时间线数据正常加载
- [ ] 窗口事件显示蓝色标签，聊天事件显示紫色标签
- [ ] 有 screenshot_id 的行 hover 变蓝，可点击
- [ ] 点击后截图卡片显示对应截图

## 浏览器历史

- [ ] 浏览器历史数据正常加载
- [ ] Chrome 显示蓝色 C，Edge 显示蓝色 E
- [ ] 有 screenshot_id 的行 hover 变蓝，可点击
- [ ] 点击后截图卡片显示对应截图

## 系统日志

- [ ] 日志数据正常加载
- [ ] INFO/WARNING/ERROR 标签颜色正确
- [ ] 分类标签正确显示中文

## 底部统计栏

- [ ] 在线数、截图数、事件数、网址数正确显示
- [ ] 数据自动刷新（5 秒间隔）

## 配色系统

- [ ] 右下角 🎨 按钮打开配色面板
- [ ] 切换强调色，logo、按钮、Agent 高亮同步变色
- [ ] 切换背景色，页面背景同步变色
- [ ] 刷新页面后配色保持（localStorage）
- [ ] 点击外部关闭面板

## 快捷键

- [ ] `⌘L` 切换实时/历史模式
- [ ] `⌘G` 打开/关闭网格视图
- [ ] `⌘R` 刷新数据
- [ ] `ESC` 关闭所有 overlay/面板

## 后端 API

- [ ] `/api/agents` 正常返回
- [ ] `/api/screenshots/latest` 正常返回
- [ ] `/api/screenshots` 分页正常
- [ ] `/api/screenshots/delete-batch` 删除成功
- [ ] `/api/app_events` 含 `screenshot_id` 字段
- [ ] `/api/browser_history` 含 `screenshot_id` 字段
- [ ] `/api/logs` 正常返回
- [ ] `/api/dashboard/stats` 正常返回
- [ ] `/api/viewer/heartbeat` 每秒发送
